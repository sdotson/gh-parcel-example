{
  "name": "proofOfConecepts",
  "version": "1.0.0",
  "main": "index.html",
  "license": "MIT",
  "browserslist": [
    "defaults",
    "not IE 11",
    "maintained node versions"
  ],
  "scripts": {
    "start": "parcel src/index.html",
    "build": "parcel build src/index.html",
    "deploy": "npm run build && git add . && git commit -a -m 'latest build' && git push origin master",
    "deployPersonal": "npm run build && git add . && git commit -a -m 'latest build' && git push personal master"
  },
  "dependencies": {
    "parcel-bundler": "^1.12.4",
    "config": "^3.3.3",
    "firebase": "^8.2.5"
  }
}
